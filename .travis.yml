# reference: http://www.objc.io/issue-6/travis-ci.html

language: objective-c
# before_install: cd Example && pod install && cd -
script:
- xctool test -workspace Example/Firebase-RACExtensions.xcworkspace -scheme Firebase-RACExtensions -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO
